<?php
/* file: cooperators.php
 *
 * purpose: retired. /cooperators told the cooperator story -- the 1920s
 *          cornfabs, the 1929 Emerson/Beadle/Fraser letter, the Ithaca 1929
 *          photograph, "What is a maize cooperator?", and how the community is
 *          organized. All of it is on /maize_history, which absorbed it when
 *          the history pages were consolidated on 2026-09-04.
 *
 *          A permanent redirect rather than a deletion, because the legacy
 *          pages link here more than twenty times and most of those links are
 *          in files this repository does not own: the ~20 templates/community/
 *          mgec-*.bau pages each carry "Of Interest to Maize Cooperators" in a
 *          footer nav, plus /doc, /tools/update_person and the legacy home.
 *          One redirect answers all of them and cannot be missed the way
 *          hand-editing twenty server-only templates can.
 *
 *          What did NOT move to /maize_history, and now lives only on the MGC's
 *          own site: the Board of Directors roster, the committee pages under
 *          /mgc/, and the code of conduct. The membership call to action points
 *          at maizegenetics.org from the MGC card on /maize_history (Carson,
 *          2026-09-04).
 *
 *          The page itself is untouched and still reachable for reference at
 *          /community/cooperators, the same arrangement /community/nomenclature
 *          has.
 *
 * Rollback: this file is the whole route. Delete it and /cooperators serves the
 * legacy page again.
 */

  header('Location: /maize_history', true, 301);
  exit;
?>
